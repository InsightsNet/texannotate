# Core dependencies
requests>=2.28.0
pebble>=5.0.0
docker>=6.0.0

# For memory-optimized temp files on Linux
memory-tempfile>=2.2.0; sys_platform == 'linux'

# PDF parsing (for analysis)
pdfplumber>=0.9.0
PyMuPDF>=1.22.0
